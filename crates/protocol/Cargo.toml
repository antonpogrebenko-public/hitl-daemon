[package]
name = "protocol"
version = "0.2.0"
edition = "2021"
description = "Shared types between HITL daemon crates"
license = "MIT"

[dependencies]
mavlink = { workspace = true }
serde = { workspace = true }
serde_json = "1.0"
thiserror = { workspace = true }
tracing = { workspace = true }
